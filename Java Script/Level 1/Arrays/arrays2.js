// Array handbook

// Array: // forEach(), map(), filter(), reduce(), find(), sort()

// Run each function to see the output, play and learn by doing.

const arr = [1, 2, 3, 4, 5, 6, 7, 8];


// Using forEach(<function>);
const arr2 = [];

function fun(str){
    arr2.push(str*2);
}

arr.forEach(fun);
// console.log(arr2);

// map function


